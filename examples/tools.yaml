version: 0.1
input:
  team: string
output:
  first_team_name: string
  first_team_goals: string
  second_team_name: string
  second_team_goals: string
prompt: 'What was the other team and score of the most recent game of the team {team}?'
tools:
  - web
model: free
