version: 0.1
input:
  planet: string
output:
  planet: string
prompt: 'write a weather report for {planet} today'
connections:
  - type: web
  - type: storage
    provider: s3
    location: /xxx/yyy
model: free
